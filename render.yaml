version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:5.7
    container_name: smart-gym-db
    environment:
      MYSQL_ROOT_PASSWORD: 903135
      MYSQL_DATABASE: life_gym